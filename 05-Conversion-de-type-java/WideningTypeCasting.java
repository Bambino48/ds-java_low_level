// Moulage de type élargissant :
/*
 * L'élargissement de la conversion de type est également
 * connu sous le nom de conversion de type implicite dans
 * laquelle un type plus petit est converti en un type plus
 * grand, cela est effectué automatiquement par le compilateur.
 * Voici la hiérarchie de conversion de type élargie en Java :
 * byte > short > char > int > long > float > double
 * Le compilateur joue un rôle dans la conversion de type mais pas les
 * programmeurs.
 * Cela change le type des variables au moment de la compilation.
 * En outre, la conversion de type s'effectue uniquement
 * du type de données de petite taille vers le type de données de grande taille.
 */

// Exemple 1 : Moulage de type élargi