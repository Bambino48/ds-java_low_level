public class LesMotsReserves {

    // Dans cette classe nous allons faire la liste des mots clés réservés en java
    // et leurs explications

    /*
     * Abstract : Selon le dictionnaire, l'abstraction est la qualité de traiter des
     * idées plutôt que des événements.
     * 
     * Assert : Le mot-clé assert est utilisé en Java pour définir l'assertion.
     * Une assertion est une instruction en Java qui garantit l'exactitude de toutes
     * les hypothèses formulées dans le programme.
     * 
     * Boolean : Le type de données booléen est l'un des huit types de données
     * primitifs pris en charge par Java.
     * Il fournit des moyens de créer des variables de type booléen qui peuvent
     * accepter une valeur booléenne comme vraie ou fausse.
     * 
     * Break : L' instruction break dans le langage de programmation Java a les deux
     * utilisations suivantes :
     * Lorsque l' instruction break est rencontrée à l'intérieur d'une boucle,
     * la boucle est immédiatement terminée et le contrôle du programme reprend à
     * l'instruction suivante suivant la boucle.
     * Il peut être utilisé pour terminer un cas dans l' instruction switch .
     * 
     * Byte : Le type de données octet est l'un des huit types de données primitifs
     * pris en charge par Java.
     * Il fournit des moyens de créer des variables de type octet pouvant accepter
     * une valeur d'octet.
     * 
     * Case : Le mot-clé case fait partie de l'instruction switch qui permet de
     * tester l'égalité d'une variable par rapport à une liste de valeurs.
     * 
     * Catch :Une exception (ou événement exceptionnel) est un problème qui survient
     * lors de l'exécution d'un programme.
     * 
     * Char : Le type de données char est l'un des huit types de données primitifs
     * pris en charge par Java.
     * 
     * Class : Java est un langage orienté objet. En tant que langage doté de la
     * fonctionnalité orienté objet.
     * 
     * Const : Le mot-clé final est utilisé pour définir une valeur constante ou des
     * méthodes/classes finales en Java.
     * 
     * Continue : Le mot-clé continue peut être utilisé dans n’importe quelle
     * structure de contrôle de boucle.
     * 
     * Default : Le mot-clé default fait partie de l'instruction switch qui permet
     * de tester l'égalité
     * d'une variable par rapport à une liste de valeurs.
     * 
     * Do : Une boucle do...while est similaire à une boucle while, sauf qu'une
     * boucle do...while est garantie de s'exécuter au moins une fois.
     * 
     * Double : Le type de données double est l'un des huit types de données
     * primitifs pris en charge par Java.
     * 
     * If : Le type de données double est l'un des huit types de données primitifs
     * pris en charge par Java.
     * 
     * Enum : La classe Java Enum est la classe de base commune à tous les types
     * d'énumération du langage Java.
     * 
     * Extends : extends est le mot-clé utilisé pour hériter des propriétés d'une
     * classe. Voici la syntaxe du mot-clé extends.
     * 
     * Final : Le mot-clé final est utilisé pour définir une valeur constante ou des
     * méthodes/classes finales en Java.
     * 
     * Fanilly : Le mot-clé enfin est utilisé pour définir un bloc final. Le bloc
     * final suit un bloc try ou un bloc catch.
     * Un bloc de code final s'exécute toujours, quelle que soit l'occurrence d'une
     * exception.
     * 
     * Float : Le type de données float est l'un des huit types de données primitifs
     * pris en charge par Java.
     * Il fournit des moyens de créer des variables de type float pouvant accepter
     * une valeur float.
     * 
     * For : Une boucle for est une structure de contrôle de répétition qui vous
     * permet d'écrire efficacement
     * une boucle qui doit être exécutée un nombre spécifique de fois.
     * 
     * Goto : L'instruction goto n'est actuellement pas prise en charge par Java. Il
     * est conservé comme mot-clé réservé pour le futur.
     * Comme alternative, Java prend en charge les étiquettes avec les instructions
     * break et continue.
     * 
     * If : Une instruction if consiste en une expression booléenne suivie d'une ou
     * plusieurs instructions.
     * 
     * Implements : Généralement, le mot-clé Implements est utilisé avec des classes
     * pour hériter des propriétés d'une interface.
     * 
     * Import : Le clavier d'importation est utilisé dans le contexte des packages.
     * 
     * Instanceof : Le mot-clé instanceof est un opérateur utilisé uniquement pour
     * les variables de référence d'objet.
     * 
     * Int : Le type de données int est l'un des huit types de données primitifs
     * pris en charge par Java.
     * 
     * Interface : Le type de données int est l'un des huit types de données
     * primitifs pris en charge par Java.
     * 
     * Long : Le type de données long est l'un des huit types de données primitifs
     * pris en charge par Java.
     * 
     * Native
     * 
     * New
     * 
     * package : Les packages sont utilisés en Java afin d'éviter les conflits de
     * noms,
     * de contrôler l'accès, de faciliter la recherche/localisation et l'utilisation
     * des classes, des interfaces, des énumérations et des annotations, etc.
     * 
     * private : Les méthodes, variables et constructeurs déclarés privés ne sont
     * accessibles que dans la classe déclarée elle-même.
     * 
     * protected : Le modificateur d'accès protégé ne peut pas être appliqué à la
     * classe et aux interfaces.
     * 
     * public : Une classe, une méthode, un constructeur, une interface, etc.
     * déclarés publics sont accessibles depuis n'importe quelle autre classe.
     * 
     * Return
     * 
     * Short : En attribuant différents types de données aux variables, vous pouvez
     * stocker des entiers, des décimales ou des caractères dans ces variables.
     * 
     * Static : Le mot clé static est utilisé pour créer des variables qui
     * existeront indépendamment de toute instance créée pour la classe.
     * 
     * Strictfp
     * 
     * Super : Le super mot-clé est similaire à ce mot-clé.
     * 
     * Switch : Une instruction switch permet de tester l’égalité d’une variable par
     * rapport à une liste de valeurs.
     * 
     * This : ce mot-clé est un mot-clé très important pour identifier un objet.
     * Voici l'utilisation de ce mot-clé.
     * 
     * Throw : Si une méthode ne gère pas une exception vérifiée, elle doit la
     * déclarer à l'aide du mot-clé throws .
     * 
     * Transient : La sérialisation est un concept grâce auquel nous pouvons écrire
     * l'état d'un objet dans un flux
     * d'octets afin de pouvoir le transférer sur le réseau (en utilisant des
     * technologies comme JPA et RMI).
     * 
     * Try : Une méthode intercepte une exception à l’aide d’une combinaison des
     * mots-clés try et catch .
     * 
     * Void
     * 
     * Volatile
     * 
     * While : Une instruction de boucle while dans le langage de programmation Java
     * exécute à plusieurs reprises
     * une instruction cible tant qu'une condition donnée est vraie.
     */

    /*
     * L'héritage :
     * 
     * En Java, les classes peuvent être dérivées de classes.
     * Fondamentalement, si vous devez créer une nouvelle classe et qu'il existe
     * déjà une classe contenant une partie du code dont vous avez besoin,
     * il est alors possible de dériver votre nouvelle classe à partir du code déjà
     * existant.
     * Ce concept permet de réutiliser les champs et méthodes de la classe existante
     * sans avoir à réécrire le code dans une nouvelle classe.
     * Dans ce scénario, la classe existante est appelée superclasse et la classe
     * dérivée est appelée sous-classe .
     */

    /*
     * L'interfaces :
     * 
     * En langage Java, une interface peut être définie comme un contrat entre des
     * objets sur la manière de communiquer entre eux.
     * Les interfaces jouent un rôle essentiel lorsqu’il s’agit de la notion
     * d’héritage.
     * Une interface définit les méthodes qu’une classe dérivée (sous-classe) doit
     * utiliser.
     * Mais la mise en œuvre des méthodes dépend entièrement de la sous-classe.
     */

}
