// 1. Commentaire sur une seule ligne
/*
 * Le commentaire sur une seule ligne permet d'ajouter un commentaire 
 * sur une seule ligne et peut être écrit à l'aide des deux barres obliques ( //). 
 * Ces commentaires sont le moyen de commentaire le plus utilisé.
 */
/*
 * Les commentaires sur une seule ligne sont le moyen de commentaire le plus utilisé 
 * pour expliquer le but (ou pour ajouter une note textuelle) de la ligne.
 */
/* Syntaxe
 * Considérez la syntaxe ci-dessous pour écrire un commentaire sur une seule ligne en Java :
 * // comment
 */

// Exemple 1 : commentaire Java sur une seule ligne

public class CommentairesSurUneLigne {

    public static void main(String[] args) {
        
    int divisor; // Definition la variable de type int
    divisor = 12; // Initialisation de la variable en affectant la valeur 12
    // if divisor is 0 throw an exception
if (divisor == 0)   {
    throw new IllegalArgumentException("divisor cannot be zero");
                    }


        
    }
    
}
